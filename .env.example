# Frontend (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase DB (only needed for scripts/apply_migrations.py)
SUPABASE_DB_PASSWORD=

# OpenAI (used by Supabase Edge Functions; do NOT prefix with VITE_)
OPENAI_API_KEY=
# Models:
# - fast/cheap: gpt-4o-mini
# - better extraction (reasoner): gpt-5.2
OPENAI_CHAT_MODEL=gpt-5.2
# Optional (only applied for gpt-5.x): low | medium | high
OPENAI_REASONING_EFFORT=high
